<h1>Listing articles</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= link_to 'Read article', article_path(article) %></td>
    </tr>
  <% end %>
</table>
